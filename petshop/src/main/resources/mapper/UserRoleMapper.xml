<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.petshop.mapper.UserRoleMapper">

    <!-- 通用字段映射（可选） -->
    <resultMap id="BaseResultMap" type="com.petshop.entity.UserRole">
        <result column="user_id" property="userId" />
        <result column="role_code" property="roleCode" />
    </resultMap>
    <!-- 插入用户角色关联 -->
    <insert id="insert" parameterType="com.petshop.entity.UserRole">
        INSERT INTO user_role (user_id, role_code)
        VALUES (#{userId}, #{roleCode})
    </insert>

    <!-- 删除用户的所有角色关联 -->
    <delete id="deleteByUserId">
        DELETE FROM user_role
        WHERE user_id = #{userId}
    </delete>

    <!-- 根据用户ID查询角色编码列表 -->
    <select id="selectRoleCodesByUserId" resultType="java.lang.String">
        SELECT role_code
        FROM user_role
        WHERE user_id = #{userId}
    </select>

    <!-- 检查用户名是否存在 -->
    <select id="existsByUsername" resultType="boolean">
        SELECT COUNT(*) > 0
        FROM user
        WHERE user_name = #{username}
    </select>

</mapper>